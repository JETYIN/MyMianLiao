<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rl_indiv_sign"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:layout_marginTop="@dimen/card_item_spacing"
    android:background="@color/white"
    android:clickable="true"
    android:gravity="center"
    android:onClick="onClick"
    android:orientation="horizontal"
    android:paddingRight="15dp"
    app:color_magic="0">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="15dp"
        android:drawableLeft="@drawable/icon_personlity"
        android:drawablePadding="10dp"
        android:text="@string/prof_short_desc"
        android:textColor="#515151"
        android:textSize="15sp"/>

    <TextView
        android:id="@+id/tv_indev_sign"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginLeft="10dp"
        android:layout_weight="1"
        android:gravity="left"
        android:textColor="#656565"
        android:textSize="14sp"/>

    <ImageView
        android:id="@+id/iv_right_individualy"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="15dp"
        android:contentDescription="@null"
        android:src="@drawable/botton_bg_chat_arrowright"/>

</LinearLayout>